using UnityEngine;

public class upgradeStation : MonoBehaviour
{
    public void upgradeFace(playerPieceLoadout loadout)
    {
        if (loadout.face == null)
            return;

        loadout.face.upgrade();

        Debug.Log("Face upgraded to level " + loadout.face.level);
    }
}